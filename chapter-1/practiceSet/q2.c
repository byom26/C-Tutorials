# include <stdio.h>

int main() {
    int radius = 10;
    float pi = 3.14;
    printf("Area of the circle: %f", pi*radius*radius);
    int height = 4;
    printf("\nVolume of the cylinder: %f", pi*radius*radius*height);

    return 0;
}