# include <stdio.h>

int main() {
    // int a = 4;  //Type declaration instruction
    float a = 1.1;
    float b = a + 8.9; // If we are reversing the order, it will throw an error
    printf("%f", b);
    return 0;
}